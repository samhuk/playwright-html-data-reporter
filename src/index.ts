/**
 * This file defines the public API of the package. Everything here will be available from
 * the top-level package name when importing as an npm package.
 *
 * E.g. `import { HtmlReporterDataReporter } from 'playwright-html-data-reporter`
 */
import { HtmlReporterDataReporter } from './playwright-html-data-reporter'

export * from './types'

export default HtmlReporterDataReporter
